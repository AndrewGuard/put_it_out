
<div class="container">

  <h2><%= @post.title %> </h2>
   <p><%= @post.body %></p>
   Post by <%= @post.user.name %>

	<h3>Comments</h3>
	<%= render @post.comments %>

	<h3>Add a comment:</h3>
	<%= render "comments/form" %>
</div>
