<% cache do %>
<%= form_for([@post, @post.comments.build]) do |f| %>

<div class="field">
  <%= f.text_area :text, :rows => 10 %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
<% end %>