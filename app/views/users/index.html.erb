<div class="container">


	<h1> Hi<%= " #{@user.username.capitalize}" %>! Welcome to your profile page. </h1>

	<ul>
		<% @user.posts.each do |post| %>
			<li><%= link_to post_path(post.id) %><li>
		<% end %>
	</ul>

  <%#= link_to "Edit", :user, :method => :edit, :remote => true%>
  <li><%= link_to "Edit", edit_user_path(session[:id]),:remote => true %></li>
 

</div>

